export interface TagAutomation {
    tags: string[];
    response: string;
}

export interface InstagramMedia {
  id: string;
  caption: string;
  media_type: string;
  media_url: string;
  timestamp: string;
}
